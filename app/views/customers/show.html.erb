<%= simple_form_for(@customer) do |f| %>
  <%= f.input :client_name, as: :string %>
  <%= f.input :teeptrak_id, as: :string %>
  <%= f.button :submit, 'Modifier mes informations', class: "btn btn-info rounded-lg mb-5" %>
<% end %>
