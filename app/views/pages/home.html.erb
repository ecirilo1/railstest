<% if signed_in? %>

  <table class="front" summary="For signed-in users">
    <tr>
      <td class="main">
        <h1 class="micropost">Mes tickets</h1>
      </td>
      <td class="sidebar round">
      </td>
    </tr>
  </table>

<% else %>

<h1>Sample App</h1>
<p>
  This is the home page for the <a href=""></a> sample application.
</p>

<%= link_to "Sign up now!", signup_path, :class => "signup_button round" %>
<% end %>
